import StubPage from '@/components/StubPage'

export default function Page() {
  return (
    <StubPage
      eyebrow="Resources"
      title="How Drift works, and why it matters."
      description="The method, the customers, the security posture, the integrations — everything an advisor or CCO needs to evaluate Drift before a single conversation."
    />
  )
}
